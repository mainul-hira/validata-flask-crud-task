# Docker Environment Configuration
# This file contains environment variables for running the application in Docker

# Flask Secret Key (change in production)
SECRET_KEY=docker-secret-key-change-in-production

# Database Configuration
DB_SERVER=db
DB_USER=sa
DB_PASS=YourStrongPassw0rd
DB_NAME=BankDB

# SQL Server connection (using 'db' as the service name instead of 'localhost')
DB_URL=mssql+pyodbc://${DB_USER}:${DB_PASS}@${DB_SERVER}:1433/${DB_NAME}?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes
